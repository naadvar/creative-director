# Pre-share gap audits (6 angles, full findings)

## First session (stranger cold-start)

### 1. iOS opens cold to a bare sign-in wall Ã¢â‚¬â€ no value, no honesty contract, no way to peek  [day]
*Why before sharing:* A stranger who taps an App Store link lands on Home(), which for a logged-out native user renders ONLY <EmailGate heading="Sign in" sub="Enter your email Ã¢â‚¬â€ no password. We'll save your reads.">. There is zero copy about what the app does, and the honesty contract (the entire differentiator) lives only in the footer Disclaimer, which is `hidden ... sm:block` Ã¢â‚¬â€ invisible on every phone. So the first screen of a craft-critic app that stakes its identity on 'we never predict virality, we read your actual frames' says none of that. The stranger is asked to hand over an account (or Apple ID) to an app they know nothing about, before seeing a single read. This is the #1 install-to-first-upload leak: highest-frequency screen, worst signal-to-ask ratio.

*Where:* frontend/src/App.tsx Home() (lines ~200-223) + AnalyzeRoute() (~229-251); the EmailGate copy in frontend/src/components/EmailGate.tsx; Disclaimer hidden on mobile via App.tsx:318 `hidden border-t border-border sm:block`

*Do:* Give the native sign-in screen a real above-the-form header: one line of what it is ('An honest craft read of your reel Ã¢â‚¬â€ reads every frame, never guesses virality') + the 3-step how-it-works already written in LandingPage.tsx (reuse it). Add a 'See an example read' link (playbook item 7) that opens ONE canned corpus read read-only, so value is visible before the account ask Ã¢â‚¬â€ this is the single highest-leverage change. Surface the honesty one-liner on mobile (not only the sm+ footer).

### 2. A suppressed read on upload #1 is the app's first sentence Ã¢â‚¬â€ and it reads as failure, not honesty  [hours]
*Why before sharing:* With MAX_READ_ATTEMPTS=2 and a real suppression rate, a nontrivial share of first uploads come back suppressed. SuppressedRead (VideoPage.tsx:46) leads with 'I couldn't land a confident fix for this one.' For a returning creator who's seen good reads that's honest restraint; for a total stranger whose ONLY data point is this screen, it reads as 'the AI I just gave my email to broke / had nothing to say' after a 3-4 min wait. There's no framing that this is the honesty contract working as designed, and 'sometimes a clearer clip reads better' subtly blames their footage on first contact. First-impression damage is high because it can be the entire first impression.

*Where:* frontend/src/pages/VideoPage.tsx SuppressedRead (lines 46-74); backend suppressed branch api/routers/videos.py:234-245

*Do:* Detect first-ever read (n_reels===1 from the fingerprint already fetched on the page) and reframe the suppressed state for a first-timer: lead with the honesty positioning ('This is the part other tools skip Ã¢â‚¬â€ I won't invent a fix I can't stand behind'), keep 'What looked good', and hard-CTA to upload a different reel rather than leaving a near-dead-end. Do NOT soften the contract Ã¢â‚¬â€ reframe the first-timer's interpretation of it.

### 3. Email typo silently creates an orphan account with no recovery path (no password, no email live)  [hours]
*Why before sharing:* email_login (api/auth.py:184) find-or-creates by normalized email with zero verification; there is no password and email sending is not wired yet (blocked on domain+Resend). On web the session is a cookie and on native it's a bearer token in localStorage. If a stranger fat-fingers 'gmial.com', they get a working session on a brand-new account Ã¢â‚¬â€ then when the cookie/token is cleared, the app is reinstalled, or they switch devices, their reads are gone and there is literally no way to prove ownership or recover them. First upload cost ~$0.05 and the retention hook (Library/DNA) is silently orphaned. This is a data-loss trap baked into the conversion moment.

*Where:* api/auth.py email_login (184+) and normalize_email (109-111); frontend/src/components/EmailGate.tsx onSubmit (43-64)

*Do:* Near-term cheap mitigations: add a confirm/typo-guard on the email field (common-domain suggestion, or a soft 'is this right?' on submit), and steer native users to Sign in with Apple as the primary button (already present, stable sub, no typo surface) with email as fallback. Real fix ships with the domain+Resend work: a magic-link verifies the address so a typo can't silently create an unrecoverable account.

### 4. 3-4 min wait tells native users to 'keep this tab open' and can't resume if they leave  [day]
*Why before sharing:* AnalyzingView copy says 'Watching every frame... keep this tab open' (UploadPage.tsx:131) Ã¢â‚¬â€ there is no tab in a native app, and phone users WILL background the app during a multi-minute wait (a notification, a glance away). The poll loop lives in React component state, so backgrounding/killing the app loses the job handle entirely; on return the user re-enters an idle upload screen with no sign their read finished, and re-uploading burns another read + compute. For a stranger's very first upload this is the classic 'I closed it, guess it failed' bounce, made worse because the wait is uniquely long here.

*Where:* frontend/src/pages/UploadPage.tsx AnalyzingView copy (131) + the in-component poll loop (233-271); ties to playbook item 6 (resume-on-return)

*Do:* Fix the copy for native ('You can leave Ã¢â‚¬â€ we'll have it ready when you're back'). Persist {job_id, video_id} to Capacitor Preferences on submit; on app open, poll once Ã¢â‚¬â€ if done show a 'Your read is ready' banner, if running re-enter the progress view. This converts the single longest, most abandonment-prone moment of the first session from a silent loss into a comeback.

### 5. Niche picker forces a choice with no explanation, and 'other' silently strips most of the value  [hours]
*Why before sharing:* On the first upload the niche chips (UploadPage.tsx:381-406) default to Fitness with no label explaining why it's asked or what it changes. A non-fitness stranger must self-classify with no guidance; if they pick 'Something else' (the honest catch-all), the read quietly loses corpus comparison, niche DNA and population framing Ã¢â‚¬â€ but nothing on the picker tells them that, so a food or beauty creator outside the four corpora gets a thinner read on their one evaluation upload and can't tell it's because of a dropdown choice. The suspected-niche chip only helps AFTER the read, and only for the four supported niches.

*Where:* frontend/src/pages/UploadPage.tsx NICHES (9-17) and picker (381-406); backend ALLOWED_NICHES incl. 'other' in api/routers/upload.py:39

*Do:* Add one line of microcopy under 'Your niche' ('so I can compare against reels like yours Ã¢â‚¬â€ pick the closest'). Since the wedge is fitness, consider defaulting more explicitly and making the supported four visually primary with 'Something else' clearly secondary, so a stranger understands the tradeoff before spending their first upload.

### 6. Navigating to a just-finished read can hit 'isn't ready yet' with no auto-retry  [hours]
*Why before sharing:* doUpload navigates to /video/{id} the instant status flips to done (UploadPage.tsx:257), but the craft-read endpoint reads from the durable Upload row written late in _run_job. If the read pipeline produced nothing (read is None Ã¢â€ â€™ available:false, not suppressed), VideoPage falls to the plain 'The read for this reel isn't ready yet.' line (VideoPage.tsx:214-218) with no spinner and no auto-refresh Ã¢â‚¬â€ a static dead-end after a long wait. Low frequency, but when it hits it's a stranger's entire first impression and there is no recovery affordance on the page.

*Where:* frontend/src/pages/VideoPage.tsx not-ready branch (214-218); backend not-ready shape api/routers/videos.py:229-230

*Do:* Make the 'isn't ready yet' state self-heal: poll/refetch a few times with a spinner, and if still empty offer a clear 'Try reading it again' action rather than a terminal sentence. Cheap insurance against a rare-but-total first-session failure.

**Already good:** Deferred email gate on web is genuinely well-designed: pick a file freely, sign in only at the 'Read my reel' tap (UploadPage submit/gating), so the web funnel shows value before the account ask Ã¢â‚¬â€ the exact thing the native cold-open lacks. | The wait/progress UX is already hardened per the prior audit: monotonic stage stepper that never regresses on unknown backend messages, a ~4-min 'taking longer' reassurance, and a ~9-min honest give-up with 'check your Library' guidance (UploadPage.tsx AnalyzingView + poll loop). Don't re-touch these. | Same-file memoization returning the existing read instantly on a byte-identical re-upload (upload.py _find_existing_read) is a strong, honest touch Ã¢â‚¬â€ a stranger who re-uploads the same clip doesn't get a flaky different answer or a second 4-min wait. | The read page is coach-first and self-verifying: verdict Ã¢â€ â€™ What's working Ã¢â€ â€™ one 'Fix this first' lever, every timestamp tappable to jump the player (CraftRead linkify/TimeChip), copy-as-checklist, and one-tap 'not in my reel / not useful' overrides Ã¢â‚¬â€ the core payoff screen is in good shape. | Empty and early states already communicate accumulation rather than abandonment: the '0 of 3 reads' dots on Growth, the fingerprint-unlock moment, and the '{n} of 3 read' nudge on a fresh read (MyDnaPage/MyUploadsPage/VideoPage) Ã¢â‚¬â€ the retention scaffolding is solid enough to leave for launch.

## Trust surface

### 1. Native app shows zero proof before the sign-in wall  [day]
*Why before sharing:* The App Store build (the thing you are about to market, currently at zero installs) opens directly to an email/Apple sign-in (App.tsx Home + AnalyzeRoute for isNativeApp), and the Examples tab is deliberately hidden natively for IP cleanliness. A stranger who taps your launch post must hand over their identity before seeing one pixel of a read Ã¢â‚¬â€ exactly the moment they decide 'this is probably AI slop.' Web visitors get /browse; app installers get nothing.

*Where:* frontend/src/App.tsx (native Home/EmailGate branch) + a new bundled sample route, e.g. frontend/src/pages/SampleReadPage.tsx

*Do:* Bundle ONE sample read of YOUR OWN reel (owned content, so no third-party IP problem in the app): ship a static JSON craft-read + poster jpg + short mp4 (or just the poster) in the app assets, add a 'See a sample read first' link under the native sign-in card that renders the real CraftRead component read-only with tappable timestamps working against the bundled clip. Reuse the existing CraftRead/CraftScrubber components so the sample is pixel-identical to the real product. Bonus: App Store reviewers also see the product without an account.

### 2. 'Not in my reel' dismissals do not persist Ã¢â‚¬â€ the flagship trust affordance silently ignores the creator  [hours]
*Why before sharing:* CraftRead.tsx keeps dismissals in component-local useState (line 145) and GET /videos/{id}/craft-read (api/routers/videos.py craft_read) never filters by NoteFeedback. A creator flags a note as 'not in my reel', reloads or reopens the app, and the claim they explicitly said was false is back in their read. Your launch copy literally says 'Tell me where the read was wrong Ã¢â‚¬â€ that feedback tunes the model'; the product then visibly discards that correction. This is the single cheapest trust-destroying interaction to fix.

*Where:* api/routers/videos.py (craft_read) + frontend/src/components/CraftRead.tsx

*Do:* In craft_read, when the video is an Upload, query NoteFeedback rows for (video_id, reason in ['not_in_reel','not_useful']) by the upload's owner and return their note texts as dismissed_notes in the response; have CraftRead initialize its dismissed set from that list (texts match exactly Ã¢â‚¬â€ noteFeedback stores rawSpots[i] verbatim). Add a one-line acknowledgment after the tap ('Gone Ã¢â‚¬â€ and noted, thanks') mirroring the lever-feedback confirmation, instead of the note vanishing silently.

### 3. The honesty machinery is invisible: no 'how this works' page anywhere  [hours]
*Why before sharing:* The actual differentiators Ã¢â‚¬â€ two-pass fabrication gate, per-note verifier, 'we say nothing rather than invent', the empirical no-virality finding Ã¢â‚¬â€ exist only in backend code and tweet drafts. A skeptic on the site has no page to check; the suppression behavior only reveals itself if their read happens to get suppressed. LAUNCH_KIT.md already schedules a 'Why no virality promises' landing section for launch day 2 but nothing is built; every launch reply of 'how is this not GPT slop?' has no link to answer with.

*Where:* New frontend/src/pages/HowItWorksPage.tsx + route in App.tsx; links from LandingPage.tsx, the CraftRead footer line, and EmailGate sub-copy

*Do:* One static page, four sections: (1) what the model actually reads (your frames + transcript + caption Ã¢â‚¬â€ not the thumbnail), (2) the fact-check: every note passes an independent cross-check and unverifiable claims are cut, with a real screenshot of the SuppressedRead state ('when we can't ground a critique, you get this instead'), (3) why we never predict views (the one-paragraph honest version of the empirical result), (4) what your feedback taps do. Link it from the read footer text 'Read from your frames Ã¢â‚¬â€ tap any timestamp to check it' (append 'how this works'). Keep it in the web bundle so native gets it free.

### 4. Notes cite timestamps but show no visual receipt Ã¢â‚¬â€ add the frame the note refers to  [day]
*Why before sharing:* Tap-to-seek is excellent but is pull-based: the skeptic must do work to verify. In screenshots, App Store previews, and the first three seconds of scanning a read, a timestamped sentence still looks like something ChatGPT could have typed without watching. A 64px thumbnail of the exact frame next to each blind spot and the lever is instant, passive proof of watching Ã¢â‚¬â€ and it makes mis-reads self-evident, which is the honest-brand move.

*Where:* api/routers/videos.py (new GET /videos/{id}/frame.jpg?t=SECONDS) + frontend/src/components/CraftRead.tsx note rows

*Do:* cv2 is already on the lean host (upload.py uses it for _first_frame_thumbnail), so add an endpoint that seeks the archived mp4 to t seconds (CAP_PROP_POS_MSEC), writes/caches data/frames/{vid}_{t}.jpg, and serves it; 404 quietly when the local mp4 is absent (pruned corpus reels) and the UI hides the thumb. Render the thumb left of each parsed blind spot's timestamp and inside the 'Fix this first' box at lever_timestamp; tapping the thumb triggers the existing onSeek. Uploads (the trust-critical case) always have the mp4 on the volume, so coverage is 100% where it matters.

### 5. Landing page describes the product but embeds no actual read  [hours]
*Why before sharing:* The web landing (the destination of every planned tweet/Reddit link) is copy + a CTA; the nearest proof is two clicks away (browse grid, then a video page). Strangers from a 'yeah right, another AI tool' feed bounce before clicking. Your own launch thread plans to attach read screenshots Ã¢â‚¬â€ the landing should show the same thing without a click.

*Where:* frontend/src/pages/LandingPage.tsx

*Do:* Embed one hand-picked real read below the fold: poster frame on the left, a trimmed CraftRead render (verdict, one 'What's working' line, the Fix-this-first box with its timestamp chip) on the right, captioned 'A real read Ã¢â‚¬â€ every timestamp is a moment in the actual reel.' Use your own reel or one corpus example, statically imported JSON so it costs no API call and never changes under you. Link it to the full /video/{id} page. Fold in the LAUNCH_KIT day-2 'Why no virality promises' blurb directly beneath it.

### 6. ShareCard is text-only Ã¢â‚¬â€ the growth artifact looks like generic AI output  [hours]
*Why before sharing:* The share loop is your only zero-budget distribution, and the PNG it produces (ShareCard.tsx) is a dark card of pure text: no frame, no timestamp anchor. Reshared into a feed it is indistinguishable from a screenshotted chatbot answer, so it markets against the groundedness claim. The launch plan depends on these screenshots looking like receipts.

*Where:* frontend/src/components/ShareCard.tsx

*Do:* Add the reel's poster (existing /videos/{id}/thumbnail Ã¢â‚¬â€ or the frame-at-lever-timestamp endpoint from the frame-receipt finding) as a small rounded image in the card, with the lever's timestamp rendered as a chip on/next to it ('0:07 Ã¢â‚¬â€ fix this first'). Fetch the image as a blob and inline it as a data URI before html-to-image renders, so canvas tainting never breaks toPng. Keep the verdict trim; the frame is the proof.

### 7. The fact-check pass leaves no visible trace on clean reads  [day]
*Why before sharing:* The verifier that drops ungrounded notes only logs its work (craft_xray.py:447) and the gate result is a boolean; a normal read just asserts 'Read from your frames' in the footer, which any slop tool could also print. The strongest credibility fact you own Ã¢â‚¬â€ 'claims that failed an independent cross-check were cut before you saw this' Ã¢â‚¬â€ is currently invisible except in the rare suppressed case.

*Where:* creative_director/advice/craft_xray.py (_verify_notes, ground_and_gate) + frontend/src/components/CraftRead.tsx footer

*Do:* Stamp the read JSON at generation time: in _verify_notes set read['fact_check'] = {reviewed: len(spots), cut: dropped}; in ground_and_gate add gate: 'passed'. Render one muted footer line only when the field exists (new uploads; corpus reads without it render nothing, so no backfill): 'Fact-check: N notes reviewed, M cut as unverifiable' or, when M=0, 'Fact-check: every note cleared an independent cross-check.' Keep wording precise Ã¢â‚¬â€ cross-check against transcript/caption/an independent watch, never 'guaranteed accurate.'

### 8. The verify-it-yourself mechanic is undiscoverable on the first read  [hours]
*Why before sharing:* Tappable timestamps are the app's best anti-slop argument, but they are styled as subtle chips and explained only in an 11px footer. A first-time creator who never taps one experiences the read as unverified text Ã¢â‚¬â€ and first-session is the one upload most users will ever give you (per your own CREATOR_XP_AUDIT persona).

*Where:* frontend/src/components/CraftRead.tsx (or VideoPage.tsx), localStorage-gated

*Do:* On a user's first-ever upload read (localStorage flag), show a one-time inline callout above the first blind spot: 'Every timestamp is tappable Ã¢â‚¬â€ jump to the exact frame we mean and check us.' Optionally pulse the first TimeChip once. Dismiss forever on first tap. Log a 'timestamp_tapped' telemetry event if not already tracked so you can see whether the verify affordance is actually landing.

**Already good:** Tap-to-seek timestamps everywhere (TimeChip + linkify in CraftRead.tsx) plus scrubber marks that ARE the blind spots (CraftScrubber.tsx) Ã¢â‚¬â€ a real, self-defusing verification mechanic: a mis-read lands on the wrong frame instead of being absorbed. Leave the interaction as is. | The suppressed-read state (SuppressedRead in VideoPage.tsx + grounded=false handling in videos.py) Ã¢â‚¬â€ 'I couldn't land a confident fix... so I won't invent one' with the 'What looked good' fallback is exactly the right honest-but-not-dead-end shape. | Per-note 'not in my reel / not useful' overrides and the lever's helpful/not-quite buttons Ã¢â‚¬â€ one tap, recorded as labeled data, calm copy. The affordance design is right; only the persistence (finding above) needs fixing. | Honesty microcopy is consistent and specific across every surface Ã¢â‚¬â€ landing ('nothing observable reliably predicts views'), upload ('Private, never published. No performance or virality predictions'), read footer, Disclaimer footer. No drift toward growth-hack language anywhere in the frontend. | RevisionVerdict's three-state wording ('You fixed this' / 'Still worth another pass' / 'couldn't re-check, so I won't guess') with compare-at-timestamp and the 'verified by re-watching your new frames Ã¢â‚¬â€ tap to check it yourself' footer Ã¢â‚¬â€ the two-version verifier's honesty is surfaced correctly.

## Retention mechanics

### 1. Activate the only owned channel: buy the domain, set the Resend key, and make the read-ready email fire correctly  [hours]
*Why before sharing:* Every out-of-app pull is currently dark: email.py is fully wired but inert (no resend_api_key), and push does not exist. A launch-cohort creator who closes the tab or app after read 1 has literally zero signals that ever reach them again Ã¢â‚¬â€ read 2 depends entirely on unprompted memory. Also two bugs-in-waiting once the key is set: (a) upload.py step 5 skips the email for suppressed reads, so the walked-away user with a suppressed first read is never told their result exists; (b) it fires immediately on EVERY completed read, so an engaged user staring at the AnalyzingView gets a redundant email per read Ã¢â‚¬â€ spammy on day one.

*Where:* Resend dashboard + Railway env (RESEND_API_KEY, EMAIL_FROM, APP_BASE_URL); creative_director/notify/email.py; api/routers/upload.py step 5 (~line 645)

*Do:* Buy the domain (~$12/yr), verify it in Resend, set the env vars. Then: (1) send for suppressed reads too, with honest copy ('Your read is in Ã¢â‚¬â€ here's what looked good'); (2) don't send inline from the job Ã¢â‚¬â€ record read_completed_at and have a lightweight sender (see the cron finding) email only reads 10+ minutes old with no read_viewed event for that video_id (the Event table already logs read_viewed), so the email fires exactly when the creator actually walked away. Add List-Unsubscribe and the support@ reply-to while in there.

### 2. Give the re-check loop a persistent entry point: an 'open fixes' card on the New tab  [day]
*Why before sharing:* The strongest comeback mechanic Ã¢â‚¬â€ 'fix it, re-upload, get a verified Ã¢Å“â€œ You fixed this' Ã¢â‚¬â€ has exactly one entry point: a small link inside the lever box of one specific read's page, plus the ?prior= URL param. A creator who takes 3 days to edit in CapCut must remember the app, open Library, find the right reel, open it, scroll to the lever, and tap re-upload. Every added step leaks the exact users you most want back (they acted on the advice). Nothing on the New tab or Library says 'you have an unfinished loop'.

*Where:* api/routers/me.py (new GET /me/open-fixes or extend /me/uploads), frontend/src/pages/UploadPage.tsx (card above the dropzone), frontend/src/pages/MyUploadsPage.tsx

*Do:* Server: an upload is an 'open fix' when its read is grounded, opportunity_dimension != 'none', and no later Upload row has prior_video_id == its video_id (all fields already on the Upload model). Frontend: when the New tab loads and open fixes exist, render one card (most recent): 'Your open fix Ã¢â‚¬â€ "0:07 Ã¢â‚¬â€ hook text unreadable" on Leg day reel. Fixed it? Re-upload to check.' Tap Ã¢â€ â€™ /analyze?prior=up_xxx (the banner flow already exists). Fires on every app open until re-checked, deleted, or explicitly dismissed ('didn't change it') Ã¢â‚¬â€ that dismissal is also a free outcome label.

### 3. Ship re-check reminders as iOS LOCAL notifications now; defer APNs server push to month 2  [days]
*Why before sharing:* STRATEGY.md plans OneSignal push in week 3, but the highest-value nudge is not 'your read is ready' (reads finish in 1-3 min while the creator watches the progress screen) Ã¢â‚¬â€ it is 'you said you'd fix this 3 days ago'. That nudge is time-scheduled, not server-triggered, so it needs no APNs key, no OneSignal account, no backend, no per-device token table: @capacitor/local-notifications does it entirely on-device. Without it, the fix-apply window (days, in an editor, away from the app) has no bridge back for app users, and email alone misses everyone who ignores email.

*Where:* frontend: add @capacitor/local-notifications; schedule in VideoPage.tsx when a grounded read with a real lever renders; deep-link listener in App.tsx/main.tsx; Codemagic Build 7 + App Store review

*Do:* Concrete firing rules: after the FIRST grounded read renders, show an inline row under the lever Ã¢â‚¬â€ 'Editing later? Get a nudge in 3 days to re-check this fix' Ã¢â‚¬â€ and request notification permission only on that tap (contextual ask, high grant rate). Schedule one notification at T+72h: 'Did you apply the fix to <reel title>? Re-upload to check it landed.' Tap Ã¢â€ â€™ localNotificationActionPerformed listener Ã¢â€ â€™ navigate('/analyze?prior=up_xxx'). Cancel it when a re-check for that prior id starts or the upload is deleted; cap at one pending reminder. Keep APNs/OneSignal out of week 1-4 Ã¢â‚¬â€ local notifications + email cover both real gaps at zero infra.

### 4. The milestone ladder ends at read 3 Ã¢â‚¬â€ extend it to 5 with a self-serve Ideas unlock  [day]
*Why before sharing:* Reads 2 and 3 have a visible goal (ReadDots '2 of 3 reads to unlock your craft trend', the post-read banner). Reads 4 and 5 have nothing: the post-read DNA banner disappears at n>=3, Growth shows no next milestone, and 'Ideas from your DNA' Ã¢â‚¬â€ the built feature that would reward exactly those reads Ã¢â‚¬â€ is hard-off via SHOW_IDEAS=false. The staged-rollout condition you set (users with 5+ grounded reads) can be enforced by the product itself instead of waiting for you to flip a flag after the cohort has already churned.

*Where:* frontend/src/pages/MyDnaPage.tsx (SHOW_IDEAS + a locked teaser card), frontend/src/pages/VideoPage.tsx (post-read banner), creative_director/profile/ideas.py (add a min-reads gate; it currently serves from 1 read)

*Do:* Flip SHOW_IDEAS=true but gate server-side: compute_idea returns {ready:false, reason:'Unlocks at 5 reads Ã¢â‚¬â€ ideas need enough of your own reads to be grounded'} below 5 grounded reads (the honest empty-state rendering already exists). Growth tab shows a locked 'Your next reel' card with a 5-dot progress row; the post-read banner continues past 3: 'read 4 of 5 Ã¢â‚¬â€ one more unlocks ideas built from your reels.' This gives every read from 2 through 5 a named payoff: 1=Fingerprint, 3=Trend, 5=Ideas.

### 5. One scheduled worker for the two time-delayed emails (read-ready catch-up + Day-3 open-fix nudge)  [day]
*Why before sharing:* Even with Resend live, everything today fires synchronously inside the upload job Ã¢â‚¬â€ there is no mechanism at all for 'later'. The Day-7 re-upload nudge in STRATEGY.md has no code behind it, and web users (most of the launch cohort arrives via links, not the App Store) get no equivalent of the local-notification reminder, so their re-check loop still has no bridge.

*Where:* api/routers/tools.py (key-gated POST /tools/send-nudges), creative_director/notify/email.py (one new template), Railway cron (or a daemon thread in app.py on the always-on host); add nudged_at column to Upload in storage/models.py

*Do:* One idempotent endpoint a Railway cron hits hourly: (a) reads completed 10-30 min ago with no read_viewed event Ã¢â€ â€™ send read-ready; (b) open fixes (same query as the open-fixes card) aged 3-7 days with nudged_at null Ã¢â€ â€™ send 'Still seeing "<lever>" on <reel>? Re-upload to check your fix landed' with a deep link to /analyze?prior=..., stamp nudged_at. Hard rules: max one nudge email per user per 7 days, honor unsubscribes, never send performance language. Log each send as a telemetry event so kpis.py can show nudge Ã¢â€ â€™ re-check conversion.

### 6. Surface revision wins outside the single read page: Fixed badges in Library and a 'fixes landed' line in Growth  [hours]
*Why before sharing:* The verifier's 'Ã¢Å“â€œ You fixed this' is the product's single best retention moment Ã¢â‚¬â€ proof the loop pays Ã¢â‚¬â€ and it renders exactly once, on one page the creator may never revisit. my_uploads doesn't even return prior_video_id or revision state, so the Library grid reads as a static pile of critiques rather than a record of loops closed; Growth talks only about notes, never about verified outcomes.

*Where:* api/routers/me.py my_uploads cards (add revision_state + is_recheck), frontend/src/pages/MyUploadsPage.tsx ReadTile, frontend/src/pages/MyDnaPage.tsx

*Do:* Library tile: a small 'Ã¢Å“â€œ Fixed' pill when revision_verdict.state=='fixed' and an 'open fix' pill for grounded reads with an unresolved lever (same predicate as the open-fixes card Ã¢â‚¬â€ the Library becomes the loop's scoreboard). Growth: one factual line from existing data, '2 fixes verified landed across your re-checks' (count of state=='fixed'). No daily streaks Ã¢â‚¬â€ a verified-fix count is honest gamification grounded in the two-version verifier; a login streak is pressure the brand doesn't need.

### 7. Close the walk-away gap on read 1: resume an in-flight job on return instead of relying on the user to guess  [hours]
*Why before sharing:* The progress screen says 'keep this tab open'. If the creator backgrounds-then-kills the app (or the 9-minute give-up fires), the finished read lands silently in the Library; the only pointer is error prose saying 'check your Library'. For a first-time user that reads as 'the upload failed' Ã¢â‚¬â€ a first-session bounce on the exact upload you spent $0.05 and 3 minutes producing, before any email channel exists to recover them.

*Where:* frontend/src/pages/UploadPage.tsx (persist {job_id, video_id, started_at} to localStorage/@capacitor/preferences on submit; clear on done), frontend/src/App.tsx or Home (on-mount check)

*Do:* On app/tab open, if a stored job exists: poll it once; if done Ã¢â€ â€™ banner at the top of New/Home 'Your read finished Ã¢â‚¬â€ view it' linking to /video/{id}; if still running Ã¢â€ â€™ re-enter the AnalyzingView with elapsed time; if 404 (server restart) Ã¢â€ â€™ honest 'check your Library' banner with a button, not error text. Also make the 9-minute give-up screen render a 'Open Library' button. Kills the most likely first-session dead end for the cost of one localStorage key.

### 8. Catch unlinked revisions: offer to link a fresh upload to an open fix after the read completes  [days]
*Why before sharing:* The revision loop only exists if the creator initiates the upload from the old read (?prior=). The natural behavior Ã¢â‚¬â€ edit the reel, open the app, tap New, upload the new cut Ã¢â‚¬â€ produces an unlinked fresh read: no verdict, no 'you fixed it' moment, and a permanently lost revision label (the moat dataset). Explicit linking was the right anti-fabrication call, but it currently has no recovery path for the majority behavior.

*Where:* api/routers/upload.py or me.py (new POST /me/uploads/{vid}/recheck-against/{prior_vid} that runs verify_fix_addressed + compare_revision lazily on the two stored mp4s), frontend/src/pages/VideoPage.tsx (post-read chip)

*Do:* When a fresh (non-prior, non-idea) upload's read completes and the user has >=1 open fix in the same niche, show a dismissible chip above the read: 'New version of <prior title>? Check if that fix landed.' Tap runs the verifier server-side against the stored prior mp4 (both files are on the volume; ownership check as in _run_job) and attaches the verdict to the upload, rendering the same RevisionVerdict card. Never auto-link, never guess Ã¢â‚¬â€ the creator asserts the relationship, the frames verify it, which keeps the honesty contract intact.

**Already good:** The in-read action loop (CraftRead.tsx): verdict Ã¢â€ â€™ what's working Ã¢â€ â€™ one accented fix-first lever with copy-as-checklist, jump-to-timestamp chips, helpful/not-quite votes, and the re-upload CTA Ã¢â‚¬â€ nothing more should be added to this surface; it is complete and any extra CTA would dilute the one-lever hierarchy. | Same-file memoization (upload.py _find_existing_read): hash + engine-version + niche/caption-input keyed, success-only, with a separate dedupe quota Ã¢â‚¬â€ correct, honest, and safe to leave alone. | Early-state progress affordances: fingerprint-unlock celebration at read 1, ReadDots '2 of 3', DNA empty state with the 0-of-3 dots, and the post-read DNA banner Ã¢â‚¬â€ the CREATOR_XP_AUDIT P0s landed; the gap is after read 3, not before it. | Suppression is no longer a dead end: MAX_READ_ATTEMPTS auto-retry plus the 'what looked good' fallback page means a suppressed first read still delivers value in-app (only the email skip needs fixing). | Measurement is ready for all of this: kpis.py already computes weekly D1/D7 cohorts, the revision funnel (rechecks Ã¢â€ â€™ fixed/still/cant_verify), suppression rate, and per-event 7d counts Ã¢â‚¬â€ every nudge recommended above lands as one new event name, no new dashboard work needed.

## Shareability

### 1. The 'Ã¢Å“â€œ You fixed this' verdict Ã¢â‚¬â€ the ONE artifact to build Ã¢â‚¬â€ has no share affordance  [days]
*Why before sharing:* This is the single most brag-worthy, honesty-safe moment the product produces: a creator changed something, re-uploaded, and an AI that refuses to promise virality independently confirmed the craft change landed. It brags about CRAFT, not views Ã¢â‚¬â€ the only artifact that can spread without touching the honesty contract. Right now RevisionVerdict.tsx renders it once, on one page, with a dead-end 'see the original' text link and nothing to share. The LAUNCH_PLAYBOOK itself lists a 'Fixed Ã¢Å“â€œ share card' but files it under NEXT (100Ã¢â€ â€™1000 users) as far-future; it is buildable NOW and is your highest-leverage growth surface. Every share is a peer-credible testimonial ('this thing actually caught my mistake') that pre-sells the exact fitness creators you're DMing.

*Where:* frontend/src/components/RevisionVerdict.tsx (add share button on state==='fixed'); new frontend/src/components/FixedItCard.tsx; frontend/src/pages/VideoPage.tsx (wire it in next to existing ShareCard at line 263); reuses GET /api/videos/{id}/frame + /thumbnail (api/routers/videos.py:316,364) and verdict.prior_video_id / prior_timestamp already on the type (frontend/src/api/types.ts:119)

*Do:* Spec (mocked above): a 9:16-friendly card with the BEFORE frame (prior_video_id at prior_timestamp) and the AFTER frame (current upload at the same second) side by side, the one craft note that got fixed, a green 'Fix verified Ã¢â‚¬â€ it re-watched both cuts' badge, and the honest footer 'No views promised Ã¢â‚¬â€ craft only.' Pull both frames from the existing /frame endpoint (extend it to accept ?t=SECONDS if it doesn't already return an arbitrary second), composite with the same html-to-image toPng path ShareCard already uses. Creator-initiated only; the two real frames are the creator's own footage so there's no consent issue. This is the whole spread mechanic Ã¢â‚¬â€ the before/after IS the proof, and the verifier line IS the differentiator.

### 2. Native share is silently broken in the iOS app Ã¢â‚¬â€ no @capacitor/share plugin installed  [day]
*Why before sharing:* Your wedge creators live in the iOS app, and that is exactly where sharing an image file fails. ShareCard.share() (ShareCard.tsx:72-100) relies on navigator.share({files}) inside a Capacitor WKWebview. iOS WebKit's navigator.share support for file payloads is unreliable in an embedded webview, and package.json (line 15-30) confirms NO @capacitor/share plugin is installed Ã¢â‚¬â€ so when the web API is absent the code hits a bare catch that no-ops (line 94-95: 'user cancelled or unsupported Ã¢â‚¬â€ no-op'). The creator taps Share, nothing happens, no error, no fallback. You'd be driving people to a share button that dies on your primary platform, and you'd never know because share_tapped fires BEFORE the sheet even opens.

*Where:* frontend/src/components/ShareCard.tsx:72-100 (the share() fn); frontend/package.json dependencies (add @capacitor/share); frontend/src/api/client.ts:33 (isNative helper already exists to branch on)

*Do:* Install @capacitor/share, and in share() branch on the existing isNative() check: on native, write the PNG to a temp file (Filesystem) and call Share.share({files:[uri]}) via the plugin Ã¢â‚¬â€ the real iOS share sheet, which reliably accepts image files and offers 'Add to Story'. Keep the web navigator.share path for browser. Replace the silent catch with a real fallback (save-to-camera-roll toast) so a failed share never dead-ends. Test on the physical iPhone build already in TestFlight before any outreach.

### 3. share_tapped counts intents, not shares Ã¢â‚¬â€ you can't tell if the artifact actually spreads  [hours]
*Why before sharing:* The Playbook's whole outreach plan hinges on a Friday KPI ritual and 'surface the share CTA,' but the instrument is blind. api.track('share_tapped') fires at the TOP of share() (ShareCard.tsx:74), before render, before the OS sheet, before the user picks a destination or cancels. So the number conflates 'opened the modal' with 'actually shared,' and there's no artifact_type field Ã¢â‚¬â€ you literally cannot answer 'which card do people share?' which is the one question that decides where to invest. Save-image (the more likely path when native share fails) isn't tracked at all. Since you can't backfill telemetry (LAUNCH_KIT.md:90 flags this), a blind metric during your first-100 window is a permanently lost signal.

*Where:* api/routers/events.py:19 (_ALLOWED set); frontend/src/components/ShareCard.tsx:53 (save, untracked) and 74 (share, fires too early); creative_director/storage/telemetry.py (event vocab doc); creative_director/storage/kpis.py:103 (events_7d rollup)

*Do:* Add 'share_completed', 'share_saved', and 'share_canceled' to _ALLOWED, and pass an artifact_type prop ('fix_this_first' | 'fixed_it' | 'dna') via the existing props channel (EventBody already logs video_id; add a small typed field or fold into name). Fire share_completed only AFTER navigator.share/Share.share resolves without throwing; fire share_canceled in the catch. Track save() too. Then kpis.py events_7d already rolls it up. Cheap, and it turns the Friday ritual into a real read on what's worth spreading.

### 4. The DNA card Ã¢â‚¬â€ your most identity-expressive artifact Ã¢â‚¬â€ has zero share affordance  [day]
*Why before sharing:* MyDnaPage.tsx renders a genuinely shareable object: 'X reels in your DNA,' a one-line style summary, a craft trend with 'Ã¢â€ â€˜ moved past illegible text' pills. Creators love posting identity/'my stats' cards (Spotify Wrapped is the archetype), and this one is 100% honesty-safe Ã¢â‚¬â€ it's a factual read of the creator's own notes over time, explicitly NOT a performance claim (the page even says so at line 131-134). But there is no share button anywhere on the page, and the existing ShareCard component is hard-wired to a craft read (takes read/title/durationLabel props), so DNA can't reuse it. This is a free viral surface sitting unused Ã¢â‚¬â€ every shared DNA card is an ad that says 'I take my craft seriously and use a tool that's honest with me.'

*Where:* frontend/src/pages/MyDnaPage.tsx (no share button in the ready branch, ~line 88-136); would need a new DnaShareCard variant (ShareCard.tsx is not generic enough to reuse as-is)

*Do:* Add a 'Share my DNA' button to the DNA identity block that renders a compact card: the reel count, the style summary, and the 1-2 'moved past X' / recurring pills. It's pure text so it sidesteps the frame-fetching complexity of the Fixed-it card Ã¢â‚¬â€ cheapest to ship. But rank it BEHIND the Fixed-it card: the before/after 'it caught my mistake' story is more credible and more novel than a stats card. Build Fixed-it first, DNA second, and have both flow through the same fixed share() plumbing (finding #2) and telemetry (finding #3).

### 5. Copy affordances produce private paste-buffers, not shareable proof Ã¢â‚¬â€ no lightweight 'shareable receipt' from the read itself  [hours]
*Why before sharing:* CraftRead.tsx has good copy affordances Ã¢â‚¬â€ copyFix (line 176, an edit checklist), copyCaption (line 149) Ã¢â‚¬â€ but they write to the clipboard for the creator's OWN edit workflow (CapCut/notes). Nothing about them is designed to be seen by anyone else, and they're tracked separately (copy_checklist, copy_caption). That's correct for utility but means the ONLY visual, shareable artifact today is the 'Fix this first' ShareCard Ã¢â‚¬â€ a text-on-gradient card with no video frame in it (ShareCard.tsx renders verdict + opportunity text only, never a thumbnail). A share card with no image of the actual reel is far less compelling on a visual platform and less obviously 'about MY reel.'

*Where:* frontend/src/components/ShareCard.tsx:102-162 (the card body Ã¢â‚¬â€ no thumbnail); it already receives enough to fetch one (VideoPage passes it inside a page that has thumbnailUrl(id) from frontend/src/lib/format.ts:158)

*Do:* Add the reel's thumbnail (thumbnailUrl(videoId), endpoint exists at api/routers/videos.py:364) as a small poster in the existing 'Fix this first' ShareCard so the artifact visibly belongs to a specific reel. Low effort, makes the one share artifact you already ship materially more shareable, and de-risks the bigger Fixed-it build by proving the frame-in-card path first.

### 6. No share entry point for suppressed reads, and the share CTA is buried below the fold  [hours]
*Why before sharing:* Two smaller frictions. (1) When a read is suppressed, VideoPage shows SuppressedRead ('what looked good') but the whole share block is gated on `read` being present (VideoPage.tsx:222 `{read ? <share button> : null}`), so a creator who got an honest 'this is already working, nothing to fix' Ã¢â‚¬â€ a genuinely positive, on-brand moment Ã¢â‚¬â€ has nothing to share. (2) The 'Share this read' button sits in the next-step row at the very bottom of VideoPage (line 221-231), after the full read, DNA nudges, and niche chips Ã¢â‚¬â€ most creators never scroll to it. Peak delight is right when the read lands, not after they've read every note.

*Where:* frontend/src/pages/VideoPage.tsx:222 (share gated on read); VideoPage.tsx:221-261 (share button position, below everything)

*Do:* Rank this last Ã¢â‚¬â€ it's polish, not a new artifact. When you build the Fixed-it flow, put its share button INSIDE the RevisionVerdict card at the moment of the 'Ã¢Å“â€œ fixed' verdict, not in the bottom nav row, so it fires at peak delight. For suppressed reads, a small 'Share what worked' affordance is a nice-to-have but low priority. Don't over-invest here before the two real artifacts (Fixed-it, DNA) exist.

**Already good:** The honesty contract is already enforced inside the existing ShareCard: it has a SILENT_RE guard (ShareCard.tsx:7) that swaps the fix box for 'well-executed as is' and never renders a performance/virality claim Ã¢â‚¬â€ so the share surface can't leak a growth-hacky promise. Keep this pattern in every new card. | html-to-image (toPng) is already a dependency and the renderÃ¢â€ â€™PNGÃ¢â€ â€™navigator.share/download path is written and working on web (ShareCard.tsx:48-100). New artifacts should reuse this exact plumbing rather than introducing a canvas/server-render approach. | The frame + thumbnail infrastructure a before/after card needs already exists server-side: GET /api/videos/{id}/frame (api/routers/videos.py:316) and /thumbnail (line 364), plus thumbnailUrl helper (format.ts:158). No new media pipeline is required to build the Fixed-it card. | The RevisionVerdict data already carries both sides of a before/after (prior_video_id, prior_timestamp, prior_issue, evidence Ã¢â‚¬â€ types.ts:119) and is grounded by a two-version frame re-watch, so the 'verified' badge on a Fixed-it card would be truthful, not marketing. The hard part (trustworthy verdict) is done; only the share artifact is missing. | The events endpoint is correctly designed as a locked-down counter (name-whitelisted, anonymous-safe, always-ok Ã¢â‚¬â€ events.py:27-36) Ã¢â‚¬â€ the right shape to extend with share_completed/artifact_type without opening an ingestion surface.

## Ops readiness

### 1. No global concurrency cap Ã¢â‚¬â€ 200 uploads spawn 200 unbounded threads on one $10 instance  [day]
*Why before sharing:* api/routers/upload.py:864 does threading.Thread(target=_run_job).start() per upload with NO global semaphore Ã¢â‚¬â€ the only throttle is per-IP (10/day, upload.py:43). The blast-radius scenario IS 200 strangers arriving in a day: each upload holds a thread through a PyAV transcode (CPU/RAM-heavy for HEVC/portrait iPhone video, upload.py:177) plus concurrent Whisper (DeepInfra) + multi-call LLM reads. 20-40 simultaneous jobs on Railway Hobby (shared vCPU / ~512MB-2GB) will OOM-kill the process or swap to a crawl; because rate-limit state is in-memory (upload.py:71-72), the restart wipes every IP window so a crash-loop lets even MORE through. This is the single highest-blast-radius gap: the launch-day spike is exactly what takes the service down, and it takes down reads for everyone, not just the over-users.

*Where:* api/routers/upload.py:864 (thread spawn), :43 (UPLOADS_PER_DAY), :177 (_transcode_h264 CPU cost), :71-72 (ephemeral in-memory limiter)

*Do:* Add a process-wide BoundedSemaphore (e.g. cap 3-4 concurrent _run_job) and queue the rest, returning an honest 'in line, ~N min' status instead of running everything at once. Move the semaphore acquire around the heavy body, not the HTTP handler, so the poll endpoint stays responsive. Load-test with ~30 concurrent 60s clips before any launch push. If the transcode is the CPU hog, cap concurrent transcodes separately (1-2). Consider a global daily cap as a circuit breaker on top of the per-IP one.

### 2. No in-app account deletion Ã¢â‚¬â€ Apple 5.1.1(v) violation that risks removal, not just rejection  [day]
*Why before sharing:* Apple requires apps that create an account to let users INITIATE deletion from within the app. The codebase has only DELETE /uploads/{video_id} (per-upload, me.py:131) and an email-to-a-human path (PrivacyPage.tsx:75, IOS_SUBMISSION review notes). There is no /me/account DELETE route and no delete-account UI anywhere in frontend/src (confirmed: only MyUploadsPage matches 'Delete', and it's per-upload). The v1.0 submission apparently passed on the email-request wording, but this is enforced on updates and via spot audits Ã¢â‚¬â€ the next binary update can be rejected, or the live app pulled. With strangers arriving, it's also a GDPR/CCPA data-subject-rights gap. This is the highest-severity compliance item because the downside is your only distribution channel going dark.

*Where:* api/routers/me.py (no account-delete route; delete_upload at :131 is per-upload only), frontend/src has no delete-account UI, PrivacyPage.tsx:67-73

*Do:* Add DELETE /me/account: purge the User row, all their Uploads (+ files on the volume, reusing delete_upload's file cleanup), ConnectedAccount, NoteFeedback, and Events, then invalidate the session/token. Add a 'Delete my account' button in a Settings/profile screen behind a confirm dialog. Keep the email path as a fallback. This also makes 'delete review@apple.com on request' from your own review notes a one-tap reality.

### 3. Public dev-login backdoor is ON in production (API_ALLOW_DEV_LOGIN=true)  [hours]
*Why before sharing:* render.yaml:22 ships API_ALLOW_DEV_LOGIN="true" with the comment 'set false once real auth lands' Ã¢â‚¬â€ and real auth (Apple + email) HAS landed. When true, GET /auth/dev-login (auth.py) signs ANY anonymous visitor into a single shared demo_creator account (platform_user_id='demo') and redirects them into the app. A stranger who finds the URL (it's guessable, and /docs exposes the route) lands in a shared account: they see each other's demo state, their uploads collide, and every hit inflates login/new_user telemetry. It also serves corpus reels as 'their' reels, muddying the product story. Verify the LIVE Railway env (render.yaml is the retired Render blueprint) Ã¢â‚¬â€ but the flag pattern and default (config.py: allow_dev_login=True) both point to it being live.

*Where:* render.yaml:22, api/config.py (allow_dev_login default True), api/routers/auth.py dev_login route

*Do:* Set API_ALLOW_DEV_LOGIN=false on the live Railway service (and flip render.yaml/any deploy template) so /auth/dev-login 404s for the public. Flip the config default to False so 'unset' is safe. Keep dev-login for local only.

### 4. /health can't detect the 'reads fail silently' outage class Ã¢â‚¬â€ and read_failed alerts no one  [day]
*Why before sharing:* health.py returns a static {"status":"ok"} Ã¢â‚¬â€ it never checks the SQLite corpus loaded, R2 sync succeeded (entrypoint.sh can fall back to a stale/absent DB), or that the LLM/Whisper providers are reachable. Any uptime monitor stays green during exactly the July-class silent degradation your own code comments call out (upload.py:678: 'gated=False means perception failed... the silent-degradation mode that hid for 4 days in July'). When a job fails, upload.py logs read_failed to the telemetry DB (upload.py:693) but nothing pages you Ã¢â‚¬â€ with strangers, a provider key expiring or a bad deploy means every read fails and your first signal is a churned user, days later.

*Where:* api/routers/health.py:13 (static ok), entrypoint.sh (silent stale-DB fallback), api/routers/upload.py:678 & :693 (gated=False / read_failed with no alert)

*Do:* Make /health do a shallow real check: SELECT 1 on the corpus DB and confirm a known corpus row/count, return 503 if not. Add a cheap alert on the read_failed / gated=False signal Ã¢â‚¬â€ even a daily cron that pings you (or a Resend email to yourself) if the last-hour suppression-or-failure rate crosses a threshold. This is the difference between a 20-minute outage and a 4-day one.

### 5. KPIs count probe/test/demo accounts Ã¢â‚¬â€ launch metrics will be wrong from day one  [hours]
*Why before sharing:* kpis.py pulls User.email but applies zero exclusion (grep for example.com/probe/review@/exclude in kpis.py returns nothing). So WAU, signup cohorts, D1/D7 retention, and the uploads funnel all include: the shared demo_creator (dev-login), review@apple.com and any Apple-review accounts, and your own @example.com/@gmail test accounts. On day one when real N is tiny, these dominate Ã¢â‚¬â€ you'll read retention and activation off noise and make freemium/launch decisions on it. This is low blast-radius to users but high blast-radius to YOUR decisions, which is the point of the KPI dashboard.

*Where:* creative_director/storage/kpis.py compute_kpis() (no email/account-type filter), api/routers/tools.py:172 /tools/kpis

*Do:* Add an exclusion set in compute_kpis: drop users whose email matches @example.com / @test / a hardcoded founder list, and the account_type='DEMO' user, from active_days, cohorts, and the funnel. Cheapest correct version: a is_internal(email) helper filtered once at the top. Tag test uploads similarly so grounded/suppressed rates reflect real creators.

### 6. Support + privacy contact is a personal Gmail Ã¢â‚¬â€ single point of failure and low trust  [hours]
*Why before sharing:* PrivacyPage.tsx:3 literally carries 'TODO: swap for a dedicated support address before launch', and IOS_SUBMISSION support_url + all deletion/abuse routing point to naadvar@gmail.com. It's the only channel for the two things Apple made you promise: account deletion and abuse/removal within 24h. With strangers, a personal inbox means deletion requests get lost in spam, you can't hand off, and it reads as unpolished on the store page. It's also the operational chokepoint for the manual account-deletion promise until the in-app delete lands.

*Where:* frontend/src/pages/PrivacyPage.tsx:3, docs/IOS_SUBMISSION.md (support_url, review notes), api/routers/upload.py review-note references

*Do:* Stand up support@ (or help@) on the product domain you'll need anyway for Resend email + Meta OAuth, forward it to your inbox, and swap CONTACT in PrivacyPage plus the App Store support/review-contact fields. Pair with a lightweight label/filter so deletion/abuse mail is never missed within 24h.

### 7. Per-IP rate limit is spoofable and shares one IP behind the proxy Ã¢â‚¬â€ both failure modes hurt at 200 strangers  [hours]
*Why before sharing:* _client_ip (upload.py:111) trusts the FIRST x-forwarded-for hop with no allowlist of trusted proxies. Two problems at scale: (1) a scripted abuser sets their own X-Forwarded-For header and rotates it to bypass the 10/day cap entirely, burning your ~$0.05/read COGS and provider quota; (2) if Railway/Vercel ever presents a shared or collapsed egress IP for a burst of legit mobile users on the same carrier NAT, they can share a window and get false-positive 429s ('daily limit reached') on their first upload. Combined with the no-concurrency-cap finding, spoofed IPs are also the vector that floods the thread pool.

*Where:* api/routers/upload.py:111 _client_ip (trusts first XFF hop), :118 _rate_limited, :726 429 path

*Do:* Take the client IP from the trusted proxy's real connecting-IP header (Railway/Vercel provide one) rather than the leftmost spoofable XFF entry, or validate XFF against known proxy ranges. Once accounts are the norm, rate-limit per authenticated user_id (not just IP) so NAT'd real users aren't collectively throttled and per-account abuse is bounded. Add SKStoreReview after a 'Ã¢Å“â€œ Fixed' revision verdict for ratings velocity while you're in this file's UX-adjacent work.

**Already good:** Upload validation is solid for a v1: 200MB / 3-min caps enforced pre-decode (upload.py:41-42,753,811), files must actually decode as video, and the outer _run_job try/except reliably flips job.status='error' + logs read_failed rather than hanging the client Ã¢â‚¬â€ the failure surfaces to the poller. | Auth crypto is done right: Apple Sign-in verifies RS256 against Apple's JWKS with issuer/audience/expiry checks (auth.py verify_apple_identity_token), bearer tokens are itsdangerous-signed with the real API_SESSION_SECRET (set in .env, not the dev default), and delete_upload is correctly owner-scoped. | The honesty contract is enforced structurally, not just in copy: the grounding gate + MAX_READ_ATTEMPTS retry + suppression path (upload.py:396-449) means a low-confidence read returns 'what looked good' instead of a fabricated fix Ã¢â‚¬â€ the core trust promise holds under real uploads. | Private tester tooling is safely gated: /tools/* 404s unless API_TOOLS_KEY is set (it is, and it's a real random key), and the reel-grabber restricts downloads to Instagram/Facebook CDN hosts with a size cap Ã¢â‚¬â€ no open SSRF surface. | The email nudge and telemetry are correctly best-effort and inert-by-default: send_read_ready no-ops without a Resend key and never raises, and the /events endpoint is name-whitelisted and always returns ok Ã¢â‚¬â€ a notification or telemetry failure can never break a finished upload.

## Table stakes vs expectations

### 1. Fitness/CapCut exports can silently fail the file picker (accept + hard MIME allowlist is too narrow)  [hours]
*Why before sharing:* This is the one table-stakes basic that can kill the FIRST upload Ã¢â‚¬â€ the exact moment the whole funnel depends on. UploadPage.tsx line 374 sets accept="video/mp4,video/quicktime" and MAX_BYTES=200MB, and pick() only rejects on size. A creator whose CapCut/phone export is .webm, .m4v, HLS-wrapped, or a mp4 the OS tags with an unexpected MIME will either not see the file in the picker or get a confusing generic failure. Every competitor accepts whatever the phone exports; a bounce here is a bounce before the product ever runs. This is NOT consciously deferred anywhere Ã¢â‚¬â€ it's an unflagged gap.

*Where:* frontend/src/pages/UploadPage.tsx:19 (MAX_BYTES), :371-378 (accept + input), :184-192 (pick validation); backend upload handler in creative-director/api

*Do:* Widen accept to accept="video/*" and move validation to a friendly post-select check (duration + rough codec) rather than a hard MIME gate. If backend transcode (already DeepInfra/Whisper on mp4) chokes on a container, return a specific message ('this export isn't readable Ã¢â‚¬â€ try re-exporting as mp4') instead of a generic 500. Test with a real CapCut Android export and an iPhone .mov to confirm both pass.

### 2. No 'paste your IG/TikTok link' Ã¢â‚¬â€ correctly deferred, but the copy should pre-empt the expectation, not leave users hunting  [hours]
*Why before sharing:* Every 2026 competitor (Klap, quso, Vizard, Opus-alternatives) leads with paste-a-link. A fitness creator's mental model is 'give it my reel = paste the URL.' You've correctly ruled out auto-ingest (ToS/compliance, STRATEGY 'Do NOT auto-ingest'), and the official IG Graph API needs a Business account + FB Page + 4-6 week review to pull even your OWN media Ã¢â‚¬â€ genuinely not worth it at seed stage. The risk isn't the missing feature; it's that a link-primed creator lands, looks for a URL box, doesn't find one, and assumes the tool is half-built. That's a silent bounce the copy can prevent for free.

*Where:* frontend/src/pages/LandingPage.tsx:31-35 and UploadPage.tsx:290-293, 367-368

*Do:* Add one line of framing at the drop zone and on the landing hero: 'Works on unposted drafts too Ã¢â‚¬â€ export from CapCut/your camera roll and drop it in (no link needed).' This reframes upload-first as a FEATURE (critique before you post) rather than a missing basic. Do not build link import.

### 3. Duration limit is stated three different ways and not enforced client-side Ã¢â‚¬â€ a 3-min+ upload wastes 2-3 min then may fail  [hours]
*Why before sharing:* The copy says 'up to 3 min' (line 368), 'short-form only' (line 189 error), and product context says the pipeline targets shorts. But pick() only checks the 200MB byte cap, not duration. A creator who drops a 6-minute vlog gets accepted, waits through the full-screen 'Reading your reel' takeover, and only then hits a failure (or a degraded read on a too-long clip). Wrong-length is a common real-world first attempt, and the failure lands at the most expensive possible moment (after the wait). Cheap to catch up front.

*Where:* frontend/src/pages/UploadPage.tsx:184-192 (pick), :368 (copy '3 min'), :189 (error copy 'short-form only')

*Do:* Read video duration client-side (loadedmetadata on a hidden <video>) at file-pick and reject >~3 min immediately with a specific message. Make the three copy strings agree on one number. This turns a post-wait failure into an instant, honest 'that's longer than short-form Ã¢â‚¬â€ trim to under 3 min.'

### 4. Android absence is correctly gated Ã¢â‚¬â€ but validate the web PWA is a real fallback on Android Chrome before seeding  [hours]
*Why before sharing:* Android is deliberately deferred to month 3-4 behind iOS D7>=25% (STRATEGY), which is the right call Ã¢â‚¬â€ a second platform before retention proof just multiplies an unproven funnel, and Ladder-style iOS-only fitness apps do draw 'no Android?' complaints but survive. The load-bearing assumption is that Android fitness creators can use the web PWA at creative-director-psi.vercel.app. That path uses the same file picker with the narrow MIME allowlist (Finding 1) Ã¢â‚¬â€ Android's share/export MIME tagging is exactly where video/* vs video/mp4 bites hardest. If the web fallback quietly rejects Android exports, 'we have a web version' becomes false in practice.

*Where:* frontend PWA build; UploadPage.tsx file-accept path; STRATEGY.md month-3 Android gate

*Do:* Before any seeding post that could reach Android users, do a 10-minute manual test: open the PWA on an Android phone, export a reel from CapCut, and confirm it uploads and reads end-to-end. If it fails, Finding 1's video/* fix likely resolves it. Keep the Android-native build gated as planned; just make the web fallback honest.

### 5. TikTok niche is absent and roadmapped for month 6 Ã¢â‚¬â€ correct, but the word 'Reel' everywhere may repel TikTok-primary creators who ARE your wedge  [hours]
*Why before sharing:* TikTok upload support is consciously deferred (STRATEGY month 6, gated on fitness helpful%>=65%) because unsourced TikTok pacing advice would break the honesty contract before the corpus exists Ã¢â‚¬â€ sound deferral. But the craft read runs on frames/transcript regardless of platform, and MANY fitness creators are TikTok-first or cross-post. The product says 'Reel' in the hero, button, and 'niche' is IG-corpus-labeled. A TikTok-native creator reads 'Reel' as 'not for me' and bounces, even though the read would work fine on their vertical video. This is a positioning leak, not a missing feature.

*Where:* frontend/src/pages/LandingPage.tsx:27-35, UploadPage.tsx:286-293, NICHES list :9-17 (ig_ prefixed)

*Do:* Soften copy to 'your short' / 'your vertical video' in the hero and button while keeping the honest 'we compare against Instagram winners in your niche' caveat where the corpus benchmark actually applies. Let a TikTok creator upload and get a craft read TODAY (frames-grounded, no platform-specific pacing claim) without implying you have a TikTok corpus. Zero honesty cost, opens the door to the wedge audience.

### 6. Localization / non-English is correctly NOT a gap at this stage Ã¢â‚¬â€ leave it, but confirm the transcript path doesn't hard-fail on non-English audio  [hours]
*Why before sharing:* Localization is explicitly deferred (STRATEGY 'Do not build ... localization before 200+ WAU / D30>=20%') and that's right Ã¢â‚¬â€ a solo founder seeding English-speaking fitness creators has zero ROI on i18n now. The only real risk hiding here: the read leans transcript/caption-primary (per the grounding gate memory), and Whisper handles many languages, but if a Spanish/Portuguese fitness creator (a large short-form segment) uploads and the gate treats a non-English transcript as low-confidence and over-suppresses, they get a 'what looked good' fallback and assume the tool is broken. Worth a 15-min check, not a build.

*Where:* transcription path (DeepInfra Whisper) + grounding gate confidence logic in creative_director; scripts/tmp/retest_gate.py

*Do:* Run one non-English fitness reel through the pipeline and confirm it produces a real grounded read, not blanket suppression. If it over-suppresses, note it as a known limitation for later; do NOT build UI localization now.

### 7. Dark mode is the ONLY theme Ã¢â‚¬â€ a genuine non-issue; do not spend time adding a light mode  [hours]
*Why before sharing:* Listed only to close the loop on the table-stakes checklist: the app ships dark-by-default (index.css, no prefers-color-scheme light branch). For a 2026 creator tool this is the expected aesthetic, not a gap Ã¢â‚¬â€ Opus, CapCut, Descript and peers are dark-first. A creator will not bounce for lack of a light theme. Flagging so no one 'fixes' a non-problem.

*Where:* frontend/src/index.css (single dark theme)

*Do:* Leave as-is. Do not build a light-mode toggle at this stage; it's pure support debt with no retention upside for this audience.

**Already good:** Deferred sign-in / email gate is exactly the 2026 best practice Ã¢â‚¬â€ pick a file and see value first, sign in only at the 'Read my reel' tap (UploadPage.tsx:194-202, EmailGate at submit). This removes the single biggest onboarding-friction basic; do not add an upfront account wall. | Time-to-value is a single meaningful action (drop file -> one prioritized fix), which matches what 2026 onboarding research says wins: guide to ONE aha moment, no 12-feature tour. Landing page's 3-step explainer is oriented around that one action. | The full-screen 'Reading your reel' wait with a monotonic stage stepper, honest slow-state at 4 min, and honest give-up at 9 min (UploadPage.tsx:229-270) already handles the async-wait basic better than most competitors' spinners Ã¢â‚¬â€ and does it without a fake progress bar, consistent with the honesty contract. | Same-file memoization returning the existing read instantly on byte-identical re-upload (UploadPage.tsx:220-225) is a genuine table-stakes nicety competitors often lack, and it protects COGS. | Optional context (caption paste + follower count) is correctly tucked behind a <details> disclosure (UploadPage.tsx:422-457) so the default path stays file->read Ã¢â‚¬â€ the right call against cognitive-overload onboarding failure.

