# YouTube Data API
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-httplib2>=0.1.1

# Transient video access
yt-dlp>=2024.10.0

# Video / image
opencv-python>=4.9.0
Pillow>=10.0.0
imagehash>=4.3.1
scikit-learn>=1.3.0
numpy>=1.26.0

# OCR (face detection now uses OpenCV's Haar cascade, no extra package)
pytesseract>=0.3.10

# Audio / transcript
faster-whisper>=1.0.0
librosa>=0.10.0
soundfile>=0.12.1

# Embeddings
open-clip-torch>=2.24.0
sentence-transformers>=2.7.0
torch>=2.1.0

# DB + ORM
SQLAlchemy>=2.0.0

# Config / CLI / IO
pydantic>=2.5.0
pydantic-settings>=2.1.0
typer>=0.12.0
PyYAML>=6.0
python-dotenv>=1.0.0
httpx>=0.27.0
tenacity>=8.2.0
loguru>=0.7.0
tqdm>=4.66.0

# Data
pandas>=2.1.0

# Model
lightgbm>=4.0.0
shap>=0.44.0

# Scene detection + LLM advice narration
scenedetect>=0.6.4
anthropic>=0.69.0

# Per-second timeline decode (features/timeline.py) + upload duration probe
av>=12.0.0

# Frontend
streamlit>=1.40.0
instaloader

# API backend (FastAPI — wraps the pipeline for the React app)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Apify SDK — public-data Instagram ingestion path.
# 3.0.0 has a pydantic-validation bug that rejects valid actor-metadata
# responses (PAY_PER_EVENT entries without pricePerUnitUsd). Stay on 2.x.
apify-client>=2.5,<3

# Cloudflare R2 (S3-compatible) — persistent video corpus + deploy storage.
boto3>=1.34.0
